//! Scatter plot SVG rendering.
//!
//! Renders X-Y coordinate scatter plots with multiple series
//! and optional linear regression trendlines.

use crate::chart::{ChartData, ChartOptions};

/// Render scatter plot data as an SVG string (stub — implemented in next commit).
pub fn render(_data: &ChartData, _options: &ChartOptions) -> String {
    String::new()
}
