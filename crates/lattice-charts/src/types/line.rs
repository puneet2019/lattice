//! Line chart SVG rendering.
//!
//! Renders line charts with connected data points, optional markers,
//! multiple series support, and grid lines.

use crate::chart::{ChartData, ChartOptions};

/// Render line chart data as an SVG string (stub — implemented in next commit).
pub fn render(_data: &ChartData, _options: &ChartOptions) -> String {
    String::new()
}
