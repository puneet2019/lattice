//! Area chart SVG rendering.
//!
//! Renders area charts with filled regions under lines,
//! support for stacked variants, and semi-transparent fills.

use crate::chart::{ChartData, ChartOptions};

/// Render area chart data as an SVG string (stub — implemented in next commit).
pub fn render(_data: &ChartData, _options: &ChartOptions) -> String {
    String::new()
}
