//! Pie chart SVG rendering.
//!
//! Renders pie charts with wedge segments, labels with percentages,
//! and optional donut variant.

use crate::chart::{ChartData, ChartOptions};

/// Render pie chart data as an SVG string (stub — implemented in next commit).
pub fn render(_data: &ChartData, _options: &ChartOptions) -> String {
    String::new()
}
